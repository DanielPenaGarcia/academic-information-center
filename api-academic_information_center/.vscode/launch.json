{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "CIA",
            "skipFiles": [
                "<node_internals>/**"
            ],
            "program": "${workspaceFolder}/src/main.js",  // Ruta directa al archivo main.js
            "console": "internalConsole",              // Usar la terminal integrada para la salida
            "cwd": "${workspaceFolder}"                  // Usar el directorio base del proyecto
        }
    ]
}
